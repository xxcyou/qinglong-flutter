import 'package:flutter/material.dart';

/// 计划确认底部弹层（P6 实现）。
class PlanConfirmSheet extends StatelessWidget {
  const PlanConfirmSheet({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
